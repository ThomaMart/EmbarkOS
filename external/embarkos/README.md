# EmbarkOS External Tree

This directory contains all custom Buildroot components for EmbarkOS.

Current content:

- board definitions
- custom packages
- scripts
- configurations

The Buildroot source tree remains untouched.